using System;

namespace Cloud.Framework.Domain.Abstractions.Interfaces
{
    /// <summary>
    /// An interface to tag an object as an event.
    /// </summary>
    /// <remarks>Has no methods/properties because it's just used to tag an object as an event to avoid external dependencies.</remarks>
    public interface IEvent
    {
    }
}